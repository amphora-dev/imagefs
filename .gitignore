# Build artifacts (regenerated by build scripts / CI — never commit)
/output/
/artifacts/
/.bst/
imagefs.txz
imagefs.txz.sha256sum
patches/

# Don't commit archives / checksums produced by packaging
*.txz
*.tar.xz
*.tar.bz2
*.tar.gz
*.sha256sum

# OS files
.DS_Store
*.swp
*~
