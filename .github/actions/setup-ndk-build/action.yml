name: Setup NDK build
description: Install apt deps (leaf|graph) and resolve runner Android NDK
inputs:
  deps:
    description: "ci/install-build-deps.sh mode — leaf (single product) or graph (imagefs)"
    required: false
    default: graph
runs:
  using: composite
  steps:
    - name: Install build deps
      shell: bash
      run: bash ci/install-build-deps.sh "${{ inputs.deps }}"
    - name: Resolve NDK
      shell: bash
      run: bash ci/resolve-runner-ndk.sh
